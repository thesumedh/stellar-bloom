services:
  - type: web
    name: stellarbloom-relayer
    runtime: node
    rootDir: relayer
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: SPONSOR_SECRET
        sync: false
    healthCheckPath: /health
